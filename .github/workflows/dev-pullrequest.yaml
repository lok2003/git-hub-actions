name: Dev Pull Request Workflow
on:
    pull_request:
        branches:
            - dev
jobs:
    pull-request-job:
        runs-on: ubuntu-latest
        steps:
          - name: pull-request
            run: echo "This workflow runs because a Pull-request(PR) or update on dev."
            