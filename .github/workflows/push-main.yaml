name: Push to Main Example
on:
    push:
        branches:
            - main

jobs:
    say-hello:
        runs-on: ubuntu-latest
        steps:
            - name: say-hello
              run: echo "Hello! Workflow ran because you pushed to main"